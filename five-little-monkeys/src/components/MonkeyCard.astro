---
import { monkeyCount, removeMonkey } from "../stores/monkeyStore.js";
import { useStore } from "@nanostores/astro";

const count = useStore(monkeyCount);
---

<div
  class="p-4 bg-yellow-100 rounded-2xl shadow-md text-center hover:scale-105 transition-transform"
>
  <h2 class="text-xl font-bold mb-2">🐵 Monkey #{count}</h2>
  <p class="text-gray-700">Jumping on the bed!</p>
  <button
    class="mt-3 bg-red-400 hover:bg-red-500 text-white px-3 py-1 rounded-lg transition-colors"
    onClick={() => removeMonkey()}
  >
    One Fell Off!
  </button>
</div>
